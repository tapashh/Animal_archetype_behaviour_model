# Core math library used for fast vector calculations
numpy>=1.24

# Web framework used to expose the model as a REST API (roadmap item #2)
fastapi>=0.110

# Server that actually runs the FastAPI app
uvicorn>=0.29

# Used internally by FastAPI to validate incoming request data
pydantic>=2.0

# Optional: only needed if you want to keep using Jupyter notebooks
jupyterlab>=4.0
